services:
  # Base de données PostgreSQL
  postgres:
    image: postgres:15-alpine
    container_name: identity-provider-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
      POSTGRES_DB: identity_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

  # API Identity Provider
  identity-provider:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: identity-provider-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - JWT_SECRET=votre_secret_jwt_super_securise_2024
      - JWT_EXPIRES_IN=24h
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres123
      - DB_NAME=identity_db
      - MAX_LOGIN_ATTEMPTS=3
      - SESSION_DURATION=24h
    volumes:
      - ./src:/app/src
    depends_on:
      - postgres
    restart: unless-stopped

  # Serveur de carte offline (tileserver-gl)
  tileserver:
    image: maptiler/tileserver-gl:latest
    container_name: tileserver-gl
    ports:
      - "8080:8080"
    volumes:
      - ../carte:/data
    command: --verbose --public_url http://localhost:8080
    restart: unless-stopped

volumes:
  postgres_data:
